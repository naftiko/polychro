/target/
.DS_Store
/src/.vscode
/.vscode
dependency-reduced-pom.xml
/.github/node_modules
# Maven build output, wherever it sits — never enumerate modules one by one:
# an omitted module (or a stale directory left by a past layout) silently leaks
# build artifacts into `git add -A`.
**/target/
build-output.txt

# flatten-maven-plugin output
.flattened-pom.xml

# ─── IDE ───────────────────────────────────────────────────────────────────────
/.idea/

# SDK directories (non-Maven)
__pycache__/
*.pyc
/modules/polychro-python/.pytest_cache/
/modules/polychro-python/dist/
/modules/polychro-python/*.egg-info/
/modules/polychro-node/node_modules/
/modules/polychro-node/dist/

# Synced agent skills (fetched on demand from the agents-shared capability — never commit)
.agents/skills/
