# Baseline fixture for the bundled polychro:openapi ruleset (naftiko/polychro#83) —
# BundledRulesetRegressionTest auto-discovers this via RulesetCatalog.available(). This document
# deliberately violates several openapi.yml rules, including the ~ key-selector rule
# (openapi-paths-kebab-case, on the /Pets path key) and the oas3-only rule (oas3-api-servers) —
# mirrors modules/polychro-rulesets/src/test/resources/fixtures/openapi-oas3-violations.yml,
# validated against the real Spectral CLI during development of this ruleset.
openapi: 3.0.2
info:
  title: Test API
  version: "1.0"
tags:
  - name: Badger
  - name: Aardvark
paths:
  /Pets:
    get:
      summary: list
      responses:
        "200":
          description: ok
  /pets/{id}:
    get:
      summary: get
      responses:
        "200":
          description: ok
