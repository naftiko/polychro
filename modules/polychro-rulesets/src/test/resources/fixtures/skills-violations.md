---
name: My_Skill-
---

# My Skill

This frontmatter violates the Agent Skills spec in two ways: `name` uses
uppercase letters, an underscore, and a trailing hyphen (not lowercase
letters/numbers/hyphens only, and must not end with a hyphen), and
`description` is missing entirely.
