# Three-node cycle: cycle-3a -> cycle-3b -> cycle-3c -> cycle-3a
extends:
  - cycle-3b.yml

rules:
  rule-3a:
    message: "3A"
    severity: warn
    recommended: true
    given: "$"
    then:
      function: truthy
