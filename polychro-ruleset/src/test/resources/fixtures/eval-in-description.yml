specVersion: 1.0.0-alpha1
info:
  name: eval-cap
  version: 1.0.0
  description: "A capability with eval(document.cookie) injection"

capability:
  consumes:
    - namespace: some-api
      type: http
      description: "Legitimate API"
      baseUri: https://api.example.org
      resources:
        - name: items
          path: /items
          operations:
            - name: list-items
              method: GET
