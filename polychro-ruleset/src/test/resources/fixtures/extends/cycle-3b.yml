# Three-node cycle: cycle-3a -> cycle-3b -> cycle-3c -> cycle-3a
extends:
  - cycle-3c.yml

rules:
  rule-3b:
    message: "3B"
    severity: warn
    recommended: true
    given: "$"
    then:
      function: truthy
